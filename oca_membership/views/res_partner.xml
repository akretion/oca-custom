<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 AKRETION
     License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>

    <!-- res.partner Form (membership_extension) -->
    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.form.membership_extension.oca_membership</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="membership_extension.view_partner_form"/>
        
        <field name="arch" type="xml">
            <!-- Display target `membership_category_id` in place of history of `membership_category_ids` -->
            <xpath expr="//field[@name='membership_category_ids' and @widget='many2many_tags']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='membership_category_ids' and @widget='many2many_tags']" position="before">
                <field name="membership_category_id" />
            </xpath>
        </field>
    </record>
    
    <!-- res.partner Form (membership) -->
    <record model="ir.ui.view" id="view_partner_form">
        <field name="name">res.partner.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        
        <field name="arch" type="xml">
            <xpath expr="//group/group" position="inside">
                <field name="membership_channel_ids" widget="many2many_tags" />
            </xpath>
        </field>
    </record>

</odoo>
